[package]
name = "roc-kernel"
version = "0.1.0"
edition = "2021"
description = "Headless simulation kernel for ROOT OF CRIME. The playable game remains the static HTML/JS desk."
publish = false

[dependencies]
indexmap = "2"
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
